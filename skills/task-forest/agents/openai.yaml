interface:
  display_name: "Task Forest"
  short_description: "Maintain repo-local task DAGs and HTML exports"
  default_prompt: "Use $task-forest to close this session, update the repo-local task DAG, and export the HTML task forest."
